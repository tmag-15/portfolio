# Tyler Magarelli — Portfolio

Single-file static portfolio. `index.html` is the entire site — no build step,
no framework, no external requests (fonts embedded as data URIs, ~90KB total).

Deploys anywhere that serves static files. Currently: Vercel.
